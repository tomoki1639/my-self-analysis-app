// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
  // WSL/Linux環境でのバイナリ問題を回避するための明示
  engineType = "library"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // ★ ここにURLを戻します
}

// ... 以降の model 定義は変更なし